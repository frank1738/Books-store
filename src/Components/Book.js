import React from "react";

const Book = () => (
  <div>
    <h2 className="book-title">book</h2>
    <p className="author" />
    <button className="remove-btn" type="submit">
      remove
    </button>
  </div>
);
export default Book;
